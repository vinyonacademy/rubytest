name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      db:
        image: postgres:11-alpine

    steps:
      - uses: actions/checkout@v1
      - name: Setup Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.6.x

      - name: Install Node
        run: |
          sudo apt-get update && apt-get install -y --no-install-recommends nodejs

      - name: Bundle Install dependencies
        run: sudo gem install bundler -v 1.17.3

      - name: Install Gems
        run: sudo bundle check --path=vendor/bundle || bundle install --path=vendor/bundle --jobs=4 --retry=3
